class LabourHome extends StatelessWidget {
  const LabourHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Labour Panel")),
      body: const Center(child: Text("Labour Dashboard")),
    );
  }
}