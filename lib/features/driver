// file: driver_home.dart
import 'package:flutter/material.dart';

class DriverHome extends StatelessWidget {
  const DriverHome({Key? key}) : super(key: key); // safe for all versions

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Driver Panel"),
      ),
      body: const Center(
        child: Text("Driver Dashboard"),
      ),
    );
  }
}
